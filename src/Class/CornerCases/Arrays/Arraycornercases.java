package Class.CornerCases.Arrays;

public class Arraycornercases {
    public static void main(String[] args) {
        // check if array is null or not
        int[] arr = new int[9];
        if(arr == null){

        }

        // check if array has elements
        if(arr.length != 0 ){

        }

    }
}
