#include<bits/stdc++.h>

using namespace std;

int main(){

    // for loop
    for (int i = 0 ; i < 6; i++){
        cout << i;
    }
    return 0;

    // while loop
    int j = 0;
    while (j <= 5){
        j++;
        cout << j;
    }

    // do-while loop if u want to do something at once even condition does not match
    int k = 1
    do{
        cout << k;
        k++;
    }while(k == 0);
    return 0;
}