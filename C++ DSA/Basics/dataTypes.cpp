#include<bits/stdc++.h>

using namespace std;

int main(){

    // Integer
    int a = 10;
    long b = 100000;
    long long c = 334923942242;

    // Decimal
    float d = 2.5;
    double e = 2.3444

    // String
    string f , g;
    cin >> f >> g; // input = shubham mete >>>> f = shubham and g = mete
    cout << "full name " << f << " "<< g; 
    string one;
    getline(cin, one);
    cout << "Full Name " << one; 

    // char
    char h = 'g' // String is denoted in "" and char in ''
    

    return 0;
}